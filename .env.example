# Environment variables are split per app (production-grade layout).
#
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env
#
# See README.md for deployment env var lists per host.
